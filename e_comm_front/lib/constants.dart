// import 'package:dio/dio.dart';

const dServerName = 'localhost';
const dServerPort = 5000;
final dServerUrl = Uri(host: dServerName, port: dServerPort);
// final dCartServerUrl = dServerUrl.replace(path: 'api/cart');
// final dProductServerUrl = dServerUrl.replace(path: 'api/product');

// final dio = Dio();
